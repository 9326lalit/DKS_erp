// Fonts — CSS custom properties are now defined directly in globals.css
// to avoid build-time network requests to fonts.googleapis.com.
// All --font-* variable names are preserved for compatibility with themes.css.

export const fontVariables = "";
